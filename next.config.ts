import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Server build — deployed via Netlify's Next.js runtime (Git-connected).
     Enables server routes for the Phase 3 ask-me agent. */
};

export default nextConfig;
